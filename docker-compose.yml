    # docker run --rm --name wire-shark-nmap -e DISPLAY=${DISPLAY} --privileged -v ~/.ssh/:/root/.ssh -v /tmp/.X11-unix:/tmp/.X11-unix -v ./:/app --workdir /app -d wire wireshark -k -i any --no-promiscuous-mode

services:
  ft_malcolm:
    build: ./rsc/docker
    container_name: ft_malcolm
    volumes:
      - ./:/app
      - ~/.ssh/:/root/.ssh
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY}
    privileged: true
    working_dir: /app
    # only listen arp request
    command: wireshark -k -i any --no-promiscuous-mode -f "arp"
    networks:
      test-net:
        ipv4_address: 10.12.10.10
        mac_address: 10:12:10:10:cc:dd

  target:
    build: ./rsc/docker
    container_name: target
    volumes:
      - ./:/app
      - ~/.ssh/:/root/.ssh
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY}
    privileged: true
    working_dir: /app
    sysctls:
      - net.ipv4.conf.all.arp_accept=1
      - net.ipv4.conf.default.arp_accept=1
    # only listen arp request
    command: wireshark -k -i any --no-promiscuous-mode -f "arp"
    networks:
      test-net:
        ipv4_address: 10.12.10.22
        mac_address: 10:12:10:22:aa:bb

  poisoned:
    image: nginx:latest
    container_name: poisoned
    restart: unless-stopped
    networks:
      test-net:
        ipv4_address: 10.12.255.255
        mac_address: 10:12:12:12:12:12

  trigger:
    build: ./rsc/docker
    container_name: trigger
    # command: sleep infinity
    privileged: true
    volumes:
      - ./:/app
      - ~/.ssh/:/root/.ssh
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY}
    command: wireshark -k -i any --no-promiscuous-mode -f "arp"
    sysctls:
      - net.ipv4.conf.all.arp_accept=1
      - net.ipv4.conf.default.arp_accept=1
    networks:
      test-net:
        ipv4_address: 10.12.12.7
        mac_address: 10:12:12:07:07:07

networks:
  test-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.0.0/8
          gateway: 10.0.0.1

